import styled from 'styled-components';
import tw from 'tailwind.macro';

const Header = styled.header`
  ${tw`flex items-center justify-between px-6 pt-12 pb-2 bg-gray-100 border rounded-tl-lg`}
`;

export default Header;
