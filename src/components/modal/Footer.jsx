import styled from 'styled-components';
import tw from 'tailwind.macro';

const Footer = styled.div`
  ${tw`relative flex flex-row items-center justify-center px-16 py-2 border-t border-gray-200`}
`;

export default Footer;
