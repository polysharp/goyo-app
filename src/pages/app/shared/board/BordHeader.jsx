import styled from 'styled-components';
import tw from 'tailwind.macro';

const BoardHeader = styled.header`
  ${tw`flex items-center justify-between px-8 py-4 bg-white shadow`}
`;

export default BoardHeader;
