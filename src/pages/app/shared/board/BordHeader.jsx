import styled from 'styled-components';
import tw from 'tailwind.macro';

const BoardHeader = styled.header`
  ${tw`flex items-center justify-between px-6 pt-12 pb-2 rounded-tl-lg`}
`;

export default BoardHeader;
